// play_tone();
// gpio_reset_pin(BLINK_GPIO);
// gpio_set_direction(BLINK_GPIO, GPIO_MODE_OUTPUT);

// int stan_diody = 0; // 0 = wyłączona, 1 = włączona

    //     // Zmień fizyczny stan napięcia na pinie diody
    //     gpio_set_level(BLINK_GPIO, stan_diody);

    // vTaskDelay(2000 / portTICK_PERIOD_MS);